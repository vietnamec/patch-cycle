---
# Play 1: Generic servers (VMs + bare metal)
- name: Patch generic servers
  hosts: patch_targets
  become: true
  serial: 5
  max_fail_percentage: 10

  tasks:
    - name: Patch and reboot block
      block:
        - name: Update installed packages
          ansible.builtin.dnf:
            name: "*"
            state: latest
            update_only: true

        - name: Check if reboot is needed
          ansible.builtin.command:
            cmd: needs-restarting -r
          register: reboot_check
          changed_when: false
          failed_when: false

        - name: Reboot if necessary
          ansible.builtin.reboot:
            reboot_timeout: 600
          when: reboot_check.rc == 1

      rescue:
        - name: Revert snapshot for this VM
          community.vmware.vmware_guest_snapshot:
            hostname: "{{ vcenter_host }}"
            username: "{{ vault_vcenter_user }}"
            password: "{{ vault_vcenter_password }}"
            validate_certs: false
            datacenter: "{{ vcenter_datacenter }}"
            folder: "{{ vm_folder }}"
            name: "{{ inventory_hostname }}"
            snapshot_name: "pre-patch-{{ patch_run_id }}"
            state: revert
          delegate_to: localhost
          when: inventory_hostname in groups['rhel_vms']

        - name: Re-raise failure to count toward threshold
          ansible.builtin.fail:
            msg: "{{ inventory_hostname }} failed – snapshot reverted"

# Play 2: Kubernetes control plane (serial 1, zero tolerance)
- name: Patch k8s control plane
  hosts: k8s_control_plane
  become: true
  serial: 1
  max_fail_percentage: 0

  tasks:
    - name: Cordon node
      ansible.builtin.command:
        cmd: kubectl cordon {{ inventory_hostname }}
      changed_when: false

    - name: Drain node
      ansible.builtin.command:
        cmd: kubectl drain {{ inventory_hostname }} --ignore-daemonsets --delete-emptydir-data
      register: drain_output
      until: drain_output.rc == 0
      retries: 3
      delay: 10
      changed_when: false

    - name: Patch and reboot control plane block
      block:
        - name: Update packages
          ansible.builtin.dnf:
            name: "*"
            state: latest
            update_only: true

        - name: Check reboot
          ansible.builtin.command:
            cmd: needs-restarting -r
          register: reboot_check
          changed_when: false
          failed_when: false

        - name: Reboot if needed
          ansible.builtin.reboot:
            reboot_timeout: 600
          when: reboot_check.rc == 1

      rescue:
        - name: Fail control plane node
          ansible.builtin.fail:
            msg: "Control plane node {{ inventory_hostname }} failed"

    - name: Uncordon node
      ansible.builtin.command:
        cmd: kubectl uncordon {{ inventory_hostname }}
      changed_when: false
      when: not (ansible_failed_result is defined and ansible_failed_result.failed)

# Play 3: Kubernetes workers (batch, 10% tolerance)
- name: Patch k8s workers
  hosts: k8s_workers
  become: true
  serial: 5
  max_fail_percentage: 10

  tasks:
    - name: Cordon node
      ansible.builtin.command:
        cmd: kubectl cordon {{ inventory_hostname }}
      changed_when: false

    - name: Drain node
      ansible.builtin.command:
        cmd: kubectl drain {{ inventory_hostname }} --ignore-daemonsets --delete-emptydir-data
      register: drain_output
      until: drain_output.rc == 0
      retries: 3
      delay: 10
      changed_when: false

    - name: Patch and reboot worker block
      block:
        - name: Update packages
          ansible.builtin.dnf:
            name: "*"
            state: latest
            update_only: true

        - name: Check reboot
          ansible.builtin.command:
            cmd: needs-restarting -r
          register: reboot_check
          changed_when: false
          failed_when: false

        - name: Reboot if needed
          ansible.builtin.reboot:
            reboot_timeout: 600
          when: reboot_check.rc == 1

      rescue:
        - name: Fail worker node
          ansible.builtin.fail:
            msg: "Worker node {{ inventory_hostname }} failed"

    - name: Uncordon node
      ansible.builtin.command:
        cmd: kubectl uncordon {{ inventory_hostname }}
      changed_when: false
      when: not (ansible_failed_result is defined and ansible_failed_result.failed)
