---
- name: Grant SSH access for patching
  hosts: all
  become: true
  tasks:
    - name: Add the patch user's public key
      authorized_key:
        user: "{{ ansible_user }}"
        state: present
        key: "{{ lookup('file', ssh_public_key_path) }}"
      when: ssh_public_key_path is defined